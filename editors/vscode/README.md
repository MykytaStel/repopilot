# RepoPilot for VS Code

The extension runs the bundled local RepoPilot binary. It provides workspace
scan/review commands, Problems diagnostics, Markdown reports, and registers the
RepoPilot MCP server for VS Code agent mode.

It does not scan on save, upload source, send telemetry, or run in untrusted
workspaces.
