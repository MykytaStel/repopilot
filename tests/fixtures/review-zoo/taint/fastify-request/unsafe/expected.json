{
  "description": "Fastify request URL metadata is concatenated into SQL, so taint-lite must report the request-to-SQL flow.",
  "expect": [
    {
      "bucket": "definitely",
      "family": "taint",
      "kind": "taint.sql",
      "path": "src/routes/audit.ts",
      "headline": "untrusted input reaches raw SQL"
    }
  ]
}
