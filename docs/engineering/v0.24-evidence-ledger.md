# v0.24 Release Evidence Ledger

Status: open; planning baseline for RepoPilot 0.24 — Calibrated Trust.

Release: [v0.24 roadmap](../roadmap/v0.24.md).
Previous release: [v0.23 evidence ledger](v0.23-evidence-ledger.md).

Statuses: `open`, `in-progress`, `verified`, and `accepted`. A row closes only
with the evidence its closure criterion names; being listed here is not
evidence. Each PR updates its own row.

## Baseline (2026-09-25)

- `v0.23.0` is published and verified by digest on GitHub Releases, crates.io,
  npm (root and five platform packages), and Homebrew
  (`verify-publication.yml` run 36140497616).
- crates.io publishes through Trusted Publishing from the `release`
  environment; a no-op `publish-crates.yml` run for `v0.23.0` authenticated
  without a token and skipped the matching version (run 36158121757).
- npm Trusted Publishers for all six packages list both `release.yml` and
  `publish-npm.yml` with environment `npm` and publish permission; automatic
  tag publishing is not yet proven by a run.
- Risk formula `risk-v3`: priority is a score threshold (P0 ≥ 90, P1 ≥ 70,
  P2 ≥ 40); a MEDIUM finding can reach 94 from context signals alone.
- The committed rule scorecard has default-profile evidence for 6 of 54 rules.

## Tracked Items

| ID | Priority / kind | Status | Phase | Observation and closure requirement |
|---|---|---|---|---|
| RP24-001 | P1 release | open | 0 | Prerelease tags are not supported, so the pipeline cannot be rehearsed without publishing `latest`. Close when a `vX.Y.Z-rc.N` tag publishes npm under `next`, a crates.io prerelease, and a GitHub prerelease, leaves Homebrew untouched, and the release contract rejects a prerelease that would move `latest`. |
| RP24-002 | P1 release | open | 0 | Carries RP23-025: npm has only been published by manual dispatch. Close when a tag run publishes all six npm packages from `release.yml` with no manual step and `verify-publication` passes. |
| RP24-003 | P2 hygiene | open | 0 | `CRATES_IO_TOKEN` is unused; `HOMEBREW_TAP_TOKEN` expiry is not tracked. Close when the unused secret is removed and the release checklist records the tap-token expiry. |
| RP24-004 | P2 maintenance | open | 0 | `tree-sitter` 0.27 upgrade (#438). Close with the upgrade merged and the full zoo byte-identical. |
| RP24-005 | P1 defect | verified | A | `risk-v4` caps priority by severity (critical/high → P0, medium → P1, low → P2, info → P3) in both the scoring path and the review/baseline overlays, with a zero-weight `severity.priority-ceiling` explanation signal. `scripts/risk_calibration.py` before/after on 28 corpus entries (11 zoo repositories × 2 profiles, self-scan × 2, four real reviews): four MEDIUM findings moved P0 → P1, no other priority changed; committed as `docs/engineering/risk-v4-calibration.md`. Calibration fixture `risk-v4-calibration.json` adds the context-only MEDIUM case (score 91 → P1) and an uncapped HIGH security case (P0). |
| RP24-006 | P1 compatibility | verified | A | CHANGELOG migration note for `--fail-on-priority` and the Action gate; `risk_v4_ceiling_keeps_context_heavy_medium_findings_out_of_p0_gates` pins that a context-heavy MEDIUM finding (score ≥ 90) passes a P0 gate and fails a P1 gate. Scores, IDs, and baseline keys are unchanged; reports carry `formula_version: "risk-v4"` and stored `risk-v3` results keep their recorded version. Goldens changed only in the version string; the zoo snapshots did not change. |
| RP24-007 | P2 UX | open | B | `(limited)` coverage is shown without its cause. Close when every limit carries a named reason across all projections, pinned by fixtures. |
| RP24-008 | P2 UX | open | B | Large changes list proof obligations individually (48 on a 122-file change). Close when obligations are grouped by check role with one action per group and JSON keeps the full list. |
| RP24-009 | P2 performance | open | B | `review` took 9.4 s on a 122-file real change; budgets cover synthetic fixtures only. Close with a real-repository large-diff budget in CI and at least a 2× improvement on that case. |
| RP24-010 | P2 capability | open | C | Public-symbol contracts cover removed named exports only. Close with TS/JS signature, default-export, re-export, and exported-type changes, each with an unsafe fixture and safe guard. |
| RP24-011 | P2 capability | open | C | Rust public-item changes are not typed. Close with removed/re-signed `pub fn` and public struct field contracts using existing Rust import evidence. |
| RP24-012 | P2 evidence-gap | open | C/D | Carries RP23-008: contract families are validated on fixtures only. Close with labeled real-repository observations per family and their denominators. |
| RP24-013 | P2 evidence-gap | open | D | Carries RP23-013/RP23-010: 48 of 54 default-profile rules have no labeled evidence. Close when at least 15 rules report evidence with denominator, interval, power boundary, and reviewer model. |
| RP24-014 | P2 product-validation | open | D | Carries RP23-014: utility beyond existing checks is unmeasured. Close with one frozen, pre-registered differential packet that passes the recomputing validator, labeled exploratory. |
| RP24-015 | P2 workflow | open | E | The agent snapshot → review loop lacks stable machine reasons and a current guardrail guide. Close with the scripted agent-edit demo blocking on the unsafe change with a machine-readable reason, and MCP/CLI parity. |
| RP24-016 | P2 release | open | F | Close with the generated v0.24 release scorecard, the compatibility matrix extended to `v0.23.0`, and a digest-verified final tag. |
